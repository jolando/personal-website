import { MomentumDirective } from './momentum.directive';

describe('MomentumDirective', () => {
  it('should create an instance', () => {
    const directive = new MomentumDirective();
    expect(directive).toBeTruthy();
  });
});
